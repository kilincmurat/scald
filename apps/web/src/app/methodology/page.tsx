import { MethodologyDoc } from '@/components/exports/MethodologyDoc';

export const metadata = {
  title: 'Methodology — SCALD',
};

export default function MethodologyPage() {
  return <MethodologyDoc />;
}
