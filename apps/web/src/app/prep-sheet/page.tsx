import { PrepSheet } from '@/components/data-entry/PrepSheet';

export const metadata = {
  title: 'Preparation Sheet — SCALD',
};

export default function PrepSheetPage() {
  return <PrepSheet />;
}
